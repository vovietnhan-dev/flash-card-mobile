import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const primary = Color(0xFF5E35B1);
  static const primaryLight = Color(0xFF9162E4);
  static const primaryDark = Color(0xFF311B92);

  // Secondary Colors
  static const secondary = Color(0xFF26A69A);
  static const secondaryLight = Color(0xFF64D8CB);
  static const secondaryDark = Color(0xFF00766C);

  // Status Colors
  static const success = Color(0xFF66BB6A);
  static const warning = Color(0xFFFFB74D);
  static const error = Color(0xFFEF5350);
  static const info = Color(0xFF42A5F5);

  // Neutral Colors
  static const background = Color(0xFFF5F5F5);
  static const surface = Color(0xFFFFFFFF);
  static const textPrimary = Color(0xFF212121);
  static const textSecondary = Color(0xFF757575);
  static const divider = Color(0xFFE0E0E0);

  // Gradient Colors
  static const gradientStart = Color(0xFF5E35B1);
  static const gradientEnd = Color(0xFF9162E4);

  // Special Colors
  static const streak = Color(0xFFFF6F00);
  static const studyComplete = Color(0xFF43A047);
}
